# T-20 Граф-редактор пайплайнов

Статус: done (2026-08-17) · M2 · зависимости: T-13, T-21 (модель версий)

## Цель

Красивый визуальный редактор пайплайнов (D-62) на `@xyflow/react`.

## Модель

- Типы нод: `llm-stage` (промпт-шаблон, триплет harness/model/effort,
  depth, human/ai-output, vendor-флаги), `janitor` (команды скрипта,
  T-22), `human-gate` (текст/инструкция гейта).
- Рёбра: обычные + **loop-edge** (max_iters, поведение при исчерпании:
  эскалация-гейт). Циклы разрешены только через loop-edge.
- `entry` — пометка стартовой ноды (ровно одна).

## UI

- Канва xyflow: drag нод, соединение рёбер, мини-карта, автолейаут
  (dagre) по кнопке, snap grid.
- Инспектор ноды (правая панель): все ручки этапа формой; промпт-шаблон —
  редактор с подсветкой плейсхолдеров `{{...}}` и их автодополнением
  (список доступных из движка T-17).
- Инспектор loop-edge: max_iters, текст эскалации.
- Превью «как будет выглядеть ран» (read-only граф).

## Валидация (при сохранении, блокирующая)

- Ровно один entry; все ноды достижимы из entry; нет висячих рёбер.
- Циклы только через loop-edge (проверка: удалить loop-рёбра → граф ацикличен).
- Обязательные поля нод заполнены; artifact-контракты этапов согласованы
  (этап потребляет артефакт, который производится ранее).
- Плейсхолдеры в промптах — только известные.

## Сохранение

- Сохранить = новая версия (T-21); «клонировать» = копия с новым именем.
- Старые раны продолжают ссылаться на свою версию (неизменяемость).

## Acceptance

- Можно собрать пайплайн M1-дефолта с нуля в редакторе и запустить его.
- Невалидные конфигурации не сохраняются, ошибки подсвечены на канве.
- Редактирование версии, на которую ссылается активный ран, не влияет
  на ран.

## Итог (2026-08-17, субагент; проверено: build/vitest/oxlint зелёные,
живой смоук API — export/import/new version работают)

Сделано:
- Роут /pipelines/$id/edit (lazy-чанк, xyflow не раздувает основной бандл):
  канва @xyflow/react (ноды-этапы цепочкой по порядку массива, loop-ребро
  петлёй с подписью i/max_iters, нода «финальный гейт», MiniMap/Controls),
  инспектор ноды (key/harness/model/effort/artifact/questions_path/
  gate_after/prompt_template), редактор промпта с подсветкой
  плейсхолдеров (известные/неизвестные) + легенда, панель петли,
  добавление/удаление/перестановка/дублирование нод, janitor/human-gate
  disabled в палитре (M2).
- Валидация блокирующая с live-подсветкой на канве (пустые/дубли keys,
  loop-ссылки, max_iters, плейсхолдеры, artifact path).
- Версии: селектор, старые read-only + «взять за основу», сохранение =
  новая версия (POST /pipelines/{id}/versions), «как новый пайплайн».
- T-21 UI: Export (скачать <name>-v<N>.yaml) и Import (.yaml →
  on_conflict:new, ошибки Error{code,message}) на экране проекта и в
  редакторе.

Отступления (зафиксированы агентом): позиции нод не персистятся (в spec
негде); автолейаут dagre не ставился (линейная M1-модель); «достижимость
из entry» и «согласованность artifact-контрактов» из ТЗ-валидации не
применимы к M1-модели (нет entry, контракты потребления не формализованы).

### Дополнение 2026-08-17 (fix-task-2: F-08, фронт, субагент)

- **F-08 (валидация обязательных полей)**: `validateSpec` теперь ловит
  пустой `prompt_template` / `model` / `harness` у llm-stage — ошибка с
  привязкой к ноде (подсветка на канве тем же механизмом stageKey).
  С 2026-08-17 (fix-task-3, п.5) backend `ValidateSpec` имеет те же
  проверки обязательности (harness/model/prompt_template у llm-stage) —
  фронт и backend выровнены; human-gate/janitor без промпта
  валидны.
- **F-07 (плейсхолдеры)**: regex приведён к backend-источнику
  (`validate.go placeholderRe`): ловится любой `{{...}}`, имя должно
  точно совпасть с известным — `{{Task}}`, `{{ task }}`, `{{artifact.}}`
  теперь ошибка и на фронте.
- Тесты (`lib/pipelineModel.test.ts`, 13 шт.): все правила validateSpec
  (включая новые), round-trip parse/serialize (serialize→parse равенство,
  идемпотентность), битый JSON → null.
- Проверки: `npx vitest run` (32/32), `npm run build`, `npm run lint` —
  зелёные.

### Дополнение 2026-08-17 (fix-task-2: П.7, фронт, субагент)

- **П.7 (автодополнение плейсхолдеров)**: реализовано без библиотек.
  Ввод `{{` в textarea промпта открывает простой попап со списком
  кандидатов (KNOWN_PLACEHOLDERS — зеркало backend `validate.go
  knownPlaceholders` — плюс префикс семейства `artifact.`); список
  фильтруется по набранному префиксу, ↑/↓ — навигация, Enter/Tab/клик —
  вставка `{{name}}` с курсором за вставленным, Esc/blur — закрытие.
  Логика вынесена в `lib/promptAutocomplete.ts` (extract/filter/apply),
  покрыта vitest (9 шт.).
- Ограничения (зафиксированы): попап прибит к низу textarea (не следует
  за кареткой — для этого нужен замер координат каретки в overlay, из
  scope вышло); вставка `artifact.` дописывает `}}` сразу — имя файла
  вводится вручную внутри скобок; автодополнение не срабатывает, если
  `{{` отделено от курсора пробелом.
- Проверки: `npx vitest run` (52/52), `npm run build`, `npm run lint` —
  зелёные.
