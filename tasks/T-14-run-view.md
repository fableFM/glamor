# T-14 Экран рана: граф + панель этапа + стрим-виджет

Статус: done (2026-08-17) · M1 · зависимости: T-13

## Цель

Главный экран продукта (D-61) — 80% времени пользователя здесь.

## Раскладка

```
┌────────────────────────────────────────────────────────┐
│ header: задача · проект · ветка · состояние · токены   │
│         [⏸ stop] [⌨ interrupt&steer] [открыть в IDE]*  │
├──────────────┬─────────────────────────────────────────┤
│ ГРАФ xyflow  │ ПАНЕЛЬ ЭТАПА (tabs)                     │
│  (лев. кол.) │ [Стрим] [Артефакты] [Промпт] [Метрики]  │
├──────────────┴─────────────────────────────────────────┤
│ ЧАТ С ПАЙПЛАЙНОМ (гейты) — см. T-15                    │
└────────────────────────────────────────────────────────┘
(* «открыть в IDE» — кнопка с M1, команда из project.ide_command, D-63)
```

## Граф

- Ноды = этапы пайплайна рана; состояния: pending (серый), running
  (пульс-анимация + последние 2-3 строки стрима прямо в ноде), succeeded
  (✓), failed (✗), interrupted (⚠ + счётчик auto-resume), waiting (гейт —
  иконка пользователя).
- Loop-edge рисуется петлёй с подписью `2/4` (итерация/лимит).
- Клик по ноде → панель этапа. Keyboard: j/k — навигация по этапам (D-64).

## Стрим-виджет (критичное требование — живой стрим рассуждений)

- Вход — нормализованные события (D-41), НЕ сырой терминал.
- Рендер: `text` — через Streamdown (streaming markdown, Shiki);
  `thinking` — сворачиваемый блок «рассуждения» (по умолчанию свёрнут после
  завершения, развёрнут во время); `tool_call` — чип (иконка тулзы + имя
  файла/команды, раскрытие → аргументы/результат усечённый); `usage` — в
  метрики; `error` — красная плашка; `raw` — под спойлером «raw output».
- Автоскролл с «прилипанием» (отмотал вверх — не дёргаем); пауза/копия
  выделенного.
- Виртуализация ленты (тысячи событий на этап — react-virtuoso или
  аналог).
- Референсы: es617/claude-replay, hoangsonww/Claude-Code-Agent-Monitor
  (см. обсуждение; посмотреть их подход к рендеру thinking/tool calls).

## Табы панели

- Артефакты: список файлов этапа, просмотр (markdown — рендер, diff — с
  подсветкой; большие файлы — скачать/открыть).
- Промпт: финальный отрендеренный промпт этапа (prompt-<stage>.md).
- Метрики: длительность, токены in/out, число попыток (resume_count).

## Acceptance

- Живой ран виден в реальном времени: пульс, стрим, смена состояний без
  перезагрузки.
- Перезагрузка страницы посреди рана → состояние восстанавливается из
  журнала, стрим догоняется (проверить на ране с 5000+ событий).
- Исторический ран (завершённый) смотрится так же — вся лента из БД.

## Итог (2026-08-17, субагент; проверено: build/vitest/oxlint зелёные + живой смоук API)

Сделано: header (задача/проект/ветка/состояние/токены, stop с confirm,
resume), колонка нод-стадий (пульс running + последние 2 строки стрима,
✓/✗/⚠+resume_count, iteration-бейдж, иконка гейта, j/k), панель этапа
[Стрим/Артефакты/Промпт/Метрики]: Streamdown для text (склейка чанков),
thinking сворачиваемый, tool_call/result чипы, error плашка, raw спойлер,
автоскролл с прилипанием; догон истории через GET events с дедупом по id.
Отступления: xyflow не ставился (разрешено ТЗ, M2); streamdown вместо
react-markdown (чанк ~810kB); «открыть в IDE» — нет эндпоинта (follow-up).

### Дополнение 2026-08-17 (fix-task-2: F-01, фронт, субагент)

- **F-01 (лента обрезает историю)**: исправлено. StreamView переведён на
  виртуализацию `react-virtuoso` (рендер только видимых блоков,
  прилипание к низу — `followOutput`, исторический ран открывается
  с конца ленты — `initialTopMostItemIndex`). В `stores/streams.ts`
  cap STREAM_BUFFER_CAP=2000 теперь применяется ТОЛЬКО к live-буферу до
  REST-догона; `mergeHistory` помечает стадию (`historyLoaded`) и дальше
  история не режется — ран с 5000+ событий смотрится целиком, начало
  стадии больше не отбрасывается. StagePanel: контейнер таба «Стрим»
  больше не имеет собственного overflow (скролл — у Virtuoso).
- Тесты (`stores/streams.test.ts`, 6 шт.): >2000 событий без потерь,
  дедуп по id при пересечении истории/live, порядок по id, live-append
  после догона не режет историю, cap только до догона, восстановление
  начала после cap-дропа.
- Acceptance «ран с 5000+ событий» / «исторический ран — вся лента из
  БД» — закрыто кодом + юнит-тестами стора; живой UI-смоук на 5000+
  событий не прогонялся (нет стенда), проверка — vitest/build/lint.
- Проверки: `npx vitest run` (32/32), `npm run build`, `npm run lint` —
  зелёные; `make build` в корне — зелёный.

### Дополнение 2026-08-17 (fix-task-2: П.1, П.3, фронт, субагент)

- **П.1 (таб «Промпт» + просмотр артефактов)**: таб «Промпт» больше не
  заглушка — показывает prompt-артефакты стадии (все попытки
  `prompt-<stage>-<iter>.md`, kind="prompt"): попытка, имя файла, время,
  раскрытие с полным путём; выбор/сортировка по итерациям — чистая логика
  `lib/artifacts.ts` (vitest, 8 шт.). Старая проверка
  `endsWith("prompt-<stage>.md")` никогда не матчила реальные имена —
  исправлено. Таб «Артефакты»: строки кликабельны, раскрытие показывает
  полный путь/kind/время.
  **Ограничение (зафиксировано)**: содержимое файлов артефактов через API
  не отдаётся — в `api/openapi.yaml` нет эндпоинта чтения артефакта,
  путь — файл на диске демона (`~/.glamor/runs/<id>/`), Tauri fs-мост не
  подключён; backend по заданию не менялся. Поэтому просмотр содержимого
  (markdown-рендер) не реализован — нужен follow-up backend
  (например GET /artifacts/{id}/content); в UI на месте содержимого —
  явная пометка об этом, а не молчаливая заглушка.
- **П.3 (молчаливые catch)**: stop/resume — ошибка теперь в локальный
  стейт `actionError` и красной строкой под header'ом (паттерн ошибок
  GateChat); фоновый backfill журнала — console.error + янтарная плашка
  «лента может быть неполной» (`backfillError`).
- Проверки: `npx vitest run` (52/52), `npm run build`, `npm run lint` —
  зелёные.

### Дополнение 2026-08-17 (fix-task-4: F-01/F-02, фронт, субагент)

- **Ограничение про просмотр артефактов из дополнения fix-task-2 П.1
  снято**: появился `GET /runs/{runId}/artifacts/{artifactId}/content`
  (backend, F-02). Раскрытие строки в табах «Артефакты» и «Промпт» грузит
  содержимое (`api/client.getArtifactContent`) и рендерит: `.md` —
  через `components/Markdown.tsx`, `.diff/.patch` — моноширинный блок с
  раскраской +/- строк (без новых зависимостей), прочее — `<pre>`
  (`components/run/ArtifactContent.tsx`, выбор рендера — чистая
  `lib/artifacts.artifactRenderKind`, vitest). Заглушки-пометки
  «содержимое недоступно» убраны. Загрузка/ошибка — видимые плашки;
  413 (> 5 МБ) — плашка + кнопка «скачать» (fetch→blob→objectURL,
  токен в URL не светится). Кэширования нет: повторное раскрытие —
  повторный fetch (KISS).
- **F-01 (run.created)**: диспатчер строит карточку рана из payload
  события без REST-догона (`lib/dispatch.runFromCreatedPayload` →
  upsert в runs-стор и пустой каркас в runDetails, если деталь ещё не
  загружена). `ensureRunKnown` оставлен fallback'ом для старых ранов
  без `run.created` (обратная совместимость).
- Проверки: `npx vitest run`, `npm run build`, `npm run lint` — зелёные.
